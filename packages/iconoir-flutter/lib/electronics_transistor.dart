import 'package:flutter/widgets.dart';
import 'package:flutter_svg/flutter_svg.dart';

class ElectronicsTransistor extends StatelessWidget {
  const ElectronicsTransistor({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) => SvgPicture.asset('icons/ElectronicsTransistor.svg');
}
