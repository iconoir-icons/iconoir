import 'package:flutter/widgets.dart';
import 'package:flutter_svg/flutter_svg.dart';

class AddSelection extends StatelessWidget {
  const AddSelection({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) => SvgPicture.asset('icons/AddSelection.svg');
}
