import 'package:flutter/widgets.dart';
import 'package:flutter_svg/flutter_svg.dart';

class AddMediaVideo extends StatelessWidget {
  const AddMediaVideo({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) => SvgPicture.asset('icons/AddMediaVideo.svg');
}
