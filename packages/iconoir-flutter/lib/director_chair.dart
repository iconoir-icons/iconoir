import 'package:flutter/widgets.dart';
import 'package:flutter_svg/flutter_svg.dart';

class DirectorChair extends StatelessWidget {
  const DirectorChair({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) => SvgPicture.asset('icons/DirectorChair.svg');
}
